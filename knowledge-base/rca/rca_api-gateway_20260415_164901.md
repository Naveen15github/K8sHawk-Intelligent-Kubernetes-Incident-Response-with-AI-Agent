# K8sHawk RCA — 2026-04-15 16:49:01

## Incident
| Field      | Value |
|------------|-------|
| Object     | `api-gateway` |
| Namespace  | `production` |
| Reason     | `CrashLoopBackOff` |
| Message    | Application startup failed - port 8080 already in use |

## Severity
**HIGH** — Pod is in CrashLoopBackOff state causing service unavailability with 5xx errors affecting 30% of user requests

## Root Cause Analysis
The pod is experiencing a CrashLoopBackOff due to port 8080 already in use. Analysis of pod logs shows the Java application is trying to bind to port 8080 but it's already occupied by a previous instance of the application that didn't terminate properly. The issue started after the recent deployment that introduced a bug in the application shutdown hook, preventing it from releasing system resources. This is causing the container to fail startup and get terminated by the kubelet. The issue is impacting user authentication and session management, resulting in 5xx errors for approximately 30% of login attempts.

## Fix Applied
```
kubectl rollout restart deployment api-gateway -n production
```
Restart the deployment to ensure all pods are properly terminated and restarted, releasing system resources

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
