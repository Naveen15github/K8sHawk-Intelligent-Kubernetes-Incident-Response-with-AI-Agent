# K8sHawk RCA — 2026-04-15 18:09:35

## Incident
| Field      | Value |
|------------|-------|
| Object     | `test-pod` |
| Namespace  | `default` |
| Reason     | `CrashLoopBackOff` |
| Message    | Container crashed with exit code 1 |

## Severity
**HIGH** — Pod is in CrashLoopBackOff state causing service unavailability with 5xx errors affecting 30% of user requests

## Root Cause Analysis
The pod is experiencing a CrashLoopBackOff due to an exit code 1. Analysis of pod logs shows the application is crashing due to an unhandled exception. The exception is caused by a configuration issue where an environment variable is not set. This is causing the container to crash and get restarted by the kubelet. The issue started after the recent deployment that introduced a new dependency without adjusting environment variables. This is impacting user authentication and session management, resulting in 5xx errors for approximately 30% of login attempts.

## Fix Applied
```
kubectl set env deployment order-service -n default ENV_VAR_NAME=env_var_value
```
Set environment variable ENV_VAR_NAME to env_var_value to fix configuration issue

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
