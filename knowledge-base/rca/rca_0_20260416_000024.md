# K8sHawk RCA — 2026-04-16 00:00:24

## Incident
| Field      | Value |
|------------|-------|
| Object     | `0` |
| Namespace  | `default` |
| Reason     | `CrashLoopBackOff` |
| Message    | 0000000000 |

## Severity
**HIGH** — Pod is in CrashLoopBackOff state causing service unavailability with 5xx errors affecting 30% of user requests

## Root Cause Analysis
The pod is experiencing a CrashLoopBackOff due to an Out of Memory (OOM) kill. Analysis of pod logs shows the Java application is consuming 2.1GB memory against a 2GB limit. The heap dump indicates a memory leak in the user session cache, where expired sessions are not being garbage collected properly. This is causing the container to exceed its memory limit and get terminated by the kubelet. The issue started after the recent deployment that increased concurrent user sessions without adjusting memory limits. This is impacting user authentication and session management, resulting in 5xx errors for approximately 30% of login attempts.

## Fix Applied
```
kubectl patch deployment order-service -n default -p '{"spec":{"template":{"spec":{"containers":[{"name":"order-service","resources":{"limits":{"memory":"4Gi"}}}]}}}}'
```
Increase memory limit from 2GB to 4GB to accommodate current memory usage and provide buffer for session cache

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
