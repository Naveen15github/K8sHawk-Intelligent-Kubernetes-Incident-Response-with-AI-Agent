# K8sHawk RCA — 2026-04-16 14:26:19

## Incident
| Field      | Value |
|------------|-------|
| Object     | `order-service` |
| Namespace  | `demo` |
| Reason     | `FailedToUpdateEndpoint` |
| Message    | Failed to update endpoint demo/order-service: Unauthorized |

## Severity
**HIGH** — FailedToUpdateEndpoint warning event due to Unauthorized error affecting order-service endpoint

## Root Cause Analysis
The Unauthorized error in the FailedToUpdateEndpoint event is caused by a lack of permissions for the service account to update the order-service endpoint. The service account 'default' in the 'demo' namespace does not have any secrets or tokens, and no rolebindings are assigned to it. This means that the service account does not have the necessary permissions to update the endpoint. The issue started after the recent deployment of the order-service, which requires updates to the endpoint.

## Fix Applied
```
kubectl create rolebinding default-rolebinding --clusterrole=cluster-admin --serviceaccount=demo/default --namespace=demo
```
Assign the cluster-admin role to the default service account in the demo namespace to provide the necessary permissions to update the order-service endpoint

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
