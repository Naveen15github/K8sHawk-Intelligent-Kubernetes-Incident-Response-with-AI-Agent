# K8sHawk RCA — 2026-04-16 16:04:02

## Incident
| Field      | Value |
|------------|-------|
| Object     | `test-incident` |
| Namespace  | `default` |
| Reason     | `Failed` |
| Message    | Failed to pull image "nonexistent-image:latest": failed to pull and unpack image "docker.io/library/nonexistent-image:latest": failed to resolve reference "docker.io/library/nonexistent-image:latest": pull access denied, repository does not exist or may require authorization: server message: insufficient_scope: authorization failed |

## Severity
**HIGH** — Pod is in ImagePullBackOff state due to pull access denied for non-existent image, causing service unavailability with 5xx errors affecting user requests

## Root Cause Analysis
The pod is experiencing an ImagePullBackOff due to a non-existent image. The image 'nonexistent-image:latest' does not exist in the Docker registry, causing the pull operation to fail. This is impacting user requests, resulting in 5xx errors. The issue started when the pod was created with the non-existent image.

## Fix Applied
```
kubectl delete pod test-incident -n default
```
Delete the pod to allow recreation with a valid image

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
