# K8sHawk RCA — 2026-04-16 16:13:50

## Incident
| Field      | Value |
|------------|-------|
| Object     | `order-service-d654585df-nf242` |
| Namespace  | `demo` |
| Reason     | `Unhealthy` |
| Message    | Liveness probe failed: Get "http://10.42.0.16:8080/health": dial tcp 10.42.0.16:8080: connect: connection refused |

## Severity
**MEDIUM** — Pod is in Unhealthy state due to liveness probe failure

## Root Cause Analysis
The liveness probe failed because the pod's container 'order-service' is not listening on port 8080. The error message 'dial tcp 10.42.0.16:8080: connect: connection refused' indicates that the container is not running or not listening on the expected port. This is preventing the pod from being marked as healthy and the service from handling any requests. The issue requires either correcting the container's configuration or ensuring the container is running and listening on the expected port.

## Fix Applied
```
kubectl delete pod order-service-d654585df-nf242 -n demo
```
Delete the pod to stop retry attempts. The underlying issue (container not listening on port 8080) needs to be fixed in the container configuration or deployment specification.

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
