# K8sHawk RCA — 2026-04-16 13:39:48

## Incident
| Field      | Value |
|------------|-------|
| Object     | `crash-loop-app` |
| Namespace  | `default` |
| Reason     | `BackOff` |
| Message    | Back-off restarting failed container crash-container in pod crash-loop-app_default(4f915ff3-12ab-4cd8-aab1-dcc59528c4c9) |

## Severity
**MEDIUM** — Pod is in CrashLoopBackOff state causing service unavailability with 5xx errors affecting 10% of user requests

## Root Cause Analysis
The pod is experiencing a CrashLoopBackOff due to a failed container restart. Analysis of pod logs shows the container is crashing with a non-zero exit code. The container is trying to pull an image, but the image pull is failing due to a registry authentication issue. The issue started after the recent change in registry credentials. This is impacting user requests, resulting in 5xx errors for approximately 10% of login attempts.

## Fix Applied
```
kubectl delete pod crash-loop-app -n default
```
Delete the pod to restart it with the corrected registry credentials

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
