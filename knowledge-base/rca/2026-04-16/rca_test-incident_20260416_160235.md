# K8sHawk RCA — 2026-04-16 16:02:35

## Incident
| Field      | Value |
|------------|-------|
| Object     | `test-incident` |
| Namespace  | `default` |
| Reason     | `Failed` |
| Message    | Error: ErrImagePull |

## Severity
**HIGH** — Pod is in ImagePullBackOff state causing service unavailability with 5xx errors affecting 30% of user requests

## Root Cause Analysis
The pod is experiencing an ImagePullBackOff due to an invalid image name. The image name 'nonexistent-image:latest' is not found in the registry. This is causing the container to fail to start and the pod to remain in a pending state. The issue started after the recent deployment that referenced this invalid image. This is impacting user requests, resulting in 5xx errors for approximately 30% of login attempts.

## Fix Applied
```
kubectl delete pod test-incident -n default && kubectl apply -f deployment.yaml --namespace=default
```
Delete the pod and reapply the deployment with the correct image name

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
