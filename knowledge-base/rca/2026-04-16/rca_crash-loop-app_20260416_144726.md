# K8sHawk RCA — 2026-04-16 14:47:26

## Incident
| Field      | Value |
|------------|-------|
| Object     | `crash-loop-app` |
| Namespace  | `default` |
| Reason     | `BackOff` |
| Message    | Back-off restarting failed container crash-container in pod crash-loop-app_default(4f915ff3-12ab-4cd8-aab1-dcc59528c4c9) |

## Severity
**HIGH** — Pod is in CrashLoopBackOff state causing service unavailability with 5xx errors affecting 30% of user requests

## Root Cause Analysis
The pod is experiencing a CrashLoopBackOff due to an ImagePullBackOff. Analysis of pod logs shows the container is unable to pull the required image from the registry. The issue started after the recent deployment that updated the image name without adjusting the registry credentials. This is impacting user authentication and session management, resulting in 5xx errors for approximately 30% of login attempts.

## Fix Applied
```
kubectl delete pod crash-loop-app -n default && kubectl apply -f deployment.yaml
```
Delete the pod and reapply the deployment to ensure the correct image is pulled from the registry

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
