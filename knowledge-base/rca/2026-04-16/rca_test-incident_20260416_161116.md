# K8sHawk RCA — 2026-04-16 16:11:16

## Incident
| Field      | Value |
|------------|-------|
| Object     | `test-incident` |
| Namespace  | `default` |
| Reason     | `Failed` |
| Message    | Failed to pull image "nonexistent-image:latest": failed to pull and unpack image "docker.io/library/nonexistent-image:latest": failed to resolve reference "docker.io/library/nonexistent-image:latest": pull access denied, repository does not exist or may require authorization: server message: insufficient_scope: authorization failed |

## Severity
**HIGH** — Pod is in ImagePullBackOff state preventing service from starting

## Root Cause Analysis
The pod is experiencing an ImagePullBackOff due to an invalid image name 'nonexistent-image:latest'. The image does not exist in any accessible container registry. Kubernetes attempted to pull the image multiple times but failed each time with 'image not found' errors. This is preventing the pod from starting and the service from handling any requests. The issue requires either correcting the image name in the deployment configuration or ensuring the image is pushed to the registry.

## Fix Applied
```
kubectl delete pod test-incident -n default
```
Delete the pod to stop retry attempts. The underlying issue (invalid image name) needs to be fixed in the deployment/pod specification.

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
