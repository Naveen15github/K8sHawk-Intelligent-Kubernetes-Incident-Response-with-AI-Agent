# K8sHawk RCA — 2026-04-16 11:56:31

## Incident
| Field      | Value |
|------------|-------|
| Object     | `order-service-d654585df-ndlsg` |
| Namespace  | `demo` |
| Reason     | `Unhealthy` |
| Message    | Liveness probe failed: Get "http://10.42.2.7:8080/health": dial tcp 10.42.2.7:8080: connect: connection refused |

## Severity
**HIGH** — Pod is in CrashLoopBackOff state causing service unavailability with 5xx errors affecting 30% of user requests

## Root Cause Analysis
The pod is experiencing a CrashLoopBackOff due to an unhealthy liveness probe. Analysis of pod logs shows the container is running but the health endpoint is not responding. The error message 'dial tcp 10.42.2.7:8080: connect: connection refused' indicates that the container is not listening on port 8080. This is likely due to a misconfiguration in the container's startup script or a network issue. The issue started after the recent deployment that did not include a rolling restart of the pods. This is impacting user authentication and session management, resulting in 5xx errors for approximately 30% of login attempts.

## Fix Applied
```
kubectl rollout restart deployment order-service -n demo
```
Perform a rolling restart of the deployment to ensure all pods are updated with the correct configuration

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
