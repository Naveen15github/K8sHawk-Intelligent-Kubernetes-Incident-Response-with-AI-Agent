# K8sHawk RCA — 2026-04-16 16:14:32

## Incident
| Field      | Value |
|------------|-------|
| Object     | `order-service-d654585df-nf242` |
| Namespace  | `demo` |
| Reason     | `BackOff` |
| Message    | Back-off restarting failed container order-service in pod order-service-d654585df-nf242_demo(660397b4-3973-45a2-bc5c-fe3922176c13) |

## Severity
**HIGH** — Pod is in BackOff state preventing service from starting

## Root Cause Analysis
The pod is experiencing a BackOff due to a failed container. The liveness probe failed with a connection refused error, indicating that the container is not running or not listening on the expected port. This is preventing the pod from starting and the service from handling any requests. The issue requires either correcting the container configuration or ensuring the container is running and listening on the expected port.

## Fix Applied
```
kubectl delete pod order-service-d654585df-nf242 -n demo
```
Delete the pod to stop retry attempts. The underlying issue (liveness probe failure) needs to be fixed in the container configuration or the container needs to be restarted.

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
