# K8sHawk RCA — 2026-04-16 16:13:27

## Incident
| Field      | Value |
|------------|-------|
| Object     | `order-service-d654585df-ndlsg` |
| Namespace  | `demo` |
| Reason     | `BackOff` |
| Message    | Back-off restarting failed container order-service in pod order-service-d654585df-ndlsg_demo(90059059-07c2-4c99-825c-e5c6b3686dd5) |

## Severity
**HIGH** — Pod is in BackOff state preventing service from starting

## Root Cause Analysis
The pod is experiencing a BackOff due to a failed liveness probe. The liveness probe failed with a connection refused error, indicating that the container is not running or not listening on the specified port. This is preventing the pod from starting and the service from handling any requests. The issue requires either correcting the container configuration or ensuring the container is running and listening on the specified port.

## Fix Applied
```
kubectl delete pod order-service-d654585df-ndlsg -n demo
```
Delete the pod to stop retry attempts. The underlying issue (liveness probe failure) needs to be fixed in the container configuration or the container needs to be restarted.

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
