# K8sHawk RCA — 2026-04-16 12:42:53

## Incident
| Field      | Value |
|------------|-------|
| Object     | `order-service-d654585df-nf242` |
| Namespace  | `demo` |
| Reason     | `Unhealthy` |
| Message    | Liveness probe failed: Get "http://10.42.0.16:8080/health": dial tcp 10.42.0.16:8080: connect: connection refused |

## Severity
**HIGH** — Liveness probe failed due to connection refusal, causing service unavailability and impacting user requests

## Root Cause Analysis
The pod is experiencing a liveness probe failure due to a connection refusal. Analysis of pod logs and deployment configuration shows that the container is running an nginx image with a health check on port 8080. However, the logs indicate that the container is not listening on port 8080, causing the liveness probe to fail. The deployment configuration also shows that the container has a limited number of restarts, which can lead to the pod being terminated if the liveness probe continues to fail. This is impacting user requests and causing service unavailability.

## Fix Applied
```
kubectl patch deployment order-service -n demo -p '{"spec":{"template":{"spec":{"containers":[{"name":"order-service","livenessProbe":{"httpGet":{"path":"/","port":80}}}]}}}}'
```
Update the liveness probe to check the correct port and path

## Recovery
⚠️ Recovery not confirmed within 3 minutes

---
*Generated by K8sHawk 🦅*
